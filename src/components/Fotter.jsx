import { Typography } from "@mui/material";

const Footer = () => {
  return (
    <footer>
      <Typography variant="body2" align="center">
        
      </Typography>
    </footer>
  );
};

export default Footer;
